pefile>=2024.8.26

# task009: CHK/MPQ read+write for generating test maps. Pinned exact — chosen
# because it round-trips a full playable .scm/.scx (CHK sections + MPQ
# container via bundled StormLib), not just CHK bytes; see
# research/prior-art.md CHK row for alternatives considered.
richchk==0.3.0
