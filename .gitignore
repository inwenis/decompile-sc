# throwaway build output, logs, analysis dumps
work/scratch/

node_modules/
dist/
test-results/

# Invoke-Pester -CI writes this next to the repo root on every local CI run. It is a
# run artifact, not a source file -- it was committed by accident once and its only
# effect was a diff on every run.
testResults.xml

# Python venv created by setup.ps1
.venv/
__pycache__/

# StarCraft binaries/assets — NEVER commit game files (hard project rule)
game/

# working copies live outside the repo already; belt-and-braces in case a
# future worker symlinks/copies one in
C:/sc-work/
/sc-work/
*.exe
*.dll
*.mpq
*.MPQ
*.snp
*.pdb
*.gdb
*.rep
*.chk
*.grp
*.dat.bin

# MPQ extraction output (audio/video/gfx/fonts/string tables/data tables)
extracted/
assets/
*.wav
*.smk
*.pcx
*.fnt
*.tbl
*.spk
*.lo?
*.dat

# maps
*.scm
*.scx

# memory dumps / raw binary blobs
*.dmp
*.bin
*.mem

# IDA databases (embed the analyzed binary byte-for-byte)
*.idb
*.i64

# Ghidra
ghidra_projects/
*.gpr
*.rep/

# Ghidra headless install (task 001) -- large (~1.2 GB), pinned version + hash in
# tools/ghidra/README.md, reinstalled on demand, never committed
tools/ghidra/ghidra_*/
tools/ghidra/*.zip
