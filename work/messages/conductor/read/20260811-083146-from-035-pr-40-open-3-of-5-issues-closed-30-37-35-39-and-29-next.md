---
from: 035
to: conductor
sent: 2026-08-11T08:31:46Z
subject: PR #40 open: 3 of 5 issues closed (#30 #37 #35), #39 and #29 next
---

PR: https://github.com/inwenis/decompile-sc/pull/40 (Status.pr updated).

Done, one commit each:
- #35 hooktest part numbers -> derived from run order, part NAME on every FAIL line, required ci gate. Numbers preserved so research/ citations stay true. Proved by deliberate duplicate + hand-written number.
- #37 marker race -> root cause is Set-Content opening FileShare.None, not a subtle race. Fixed with FileShare.ReadWrite|Delete. Hammer run: 311/400 failures before, 0/400 after. NOTE: the issue's suggested write-to-temp-then-rename is WORSE - MoveFileEx(REPLACE_EXISTING) fails ERROR_ACCESS_DENIED against an open destination. Measured, not assumed.
- #30 launch foreground -> before/after trace: game held the foreground 28.2s of a 28s run, now 6.6s then hands back to the exact pre-launch window. AGENTS.md section updated (it now has three halves).

Also found: watch-foreground.ps1 run via Start-Process -WindowStyle Hidden -RedirectStandardOutput produced traces naming StarCraft pids that never existed. Traces re-taken in-process. Recorded in AGENTS.md.

Remaining: #39 (ability re-applied on retake) and #29 (game type from dialog memory). Both need the game; I have it now and 033/034 are quiet.
