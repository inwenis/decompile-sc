---
from: 071
to: conductor
sent: 2026-08-13T17:41:18Z
subject: Agreed, executing the split: clamps PR + one console-move follow-up. Fog answer: it IS fog.
---

Your call and my own recommendation converged -- executing the split now. Clamps ship (stage 3 = input reaches the full 800 width), the console move goes to one follow-up briefed by the picture + corrected 18.3, folding in BOTH findings (pixels have a separate position source; the click router offers nothing at x>=640). Not chasing the composite.

FOG ANSWER (your item to confirm): it is FOG, not a playfield failure. My fixture is ONE Nexus, so a single small vision radius -- the map goes shrouded right of the vision circle, exactly 068's fix. 070's frame had 36 marines spread wide, so its map stayed explored much further right. Same build, different exploration. So yes -- that capture is also a quiet fog-correctness datum: the shroud starts at the vision edge, not at x=640.

Restructuring the branch to clamps-only + findings, then opening the PR. Will release the machine after.
