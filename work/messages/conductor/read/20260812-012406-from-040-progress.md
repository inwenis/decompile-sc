---
from: 040
to: conductor
sent: 2026-08-12T01:24:06Z
subject: Progress
---

Windows 11 Home confirmed genuine (systeminfo/EditionID authoritative; registry ProductName field lags, cosmetic). No VM software installed; not touching that without approval.

Routes ruled OUT by desk research: Hyper-V (Home has no management stack, as expected). RDP-into-hidden-session (Home has no RDP host).

Live-testing now, no install needed: a separate Windows DESKTOP object (CreateDesktop/WinSta0, distinct from the visible one) for the game to run on. Structural proof done: created desktop is a genuinely different object from the visible input desktop, so nothing on it can composite to the screen unless something calls SwitchDesktop (nothing does). Game launches fine there and creates its window. Currently chasing a 0x0 client-rect bug in my probe's windowed-mode setup - confirmed it is NOT desktop-specific (reproduces identically on the normal visible desktop too), so it is a bug in how I'm invoking the windowed-mode shim, not a dead end for the route itself. Next: use the real injection path (scinject --early-dll) instead of the deprecated ddraw.dll-copy trick I used first.

VM (VirtualBox/VMware) stays desk-research only for now: no install attempted. If alt-desktop pans out it likely wins on cost (zero install vs a second Windows license + guest OS setup + DirectDraw-in-VM risk), so I am not planning to ask for VM install approval unless alt-desktop dies.

Not at the approval gate. Will flag immediately if that changes.
