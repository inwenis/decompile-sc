---
from: 005
to: conductor
sent: 2026-08-07T00:28:14Z
subject: Task 005 done — PR #6 open
---

PR: https://github.com/inwenis/decompile-sc/pull/6 — link is in Status.pr of work/tasks/005-xref-sweep.md. Reporting via the PR, so no work/reports/005-*.md (the task's Reporting section says either/or).

All seven acceptance criteria met. Highlights for your review gate:

1. Hash verified TWICE — Get-FileHash before the run, and Ghidra's own executableSHA256 for the imported program. Pristine install never opened.
2. Q1: 342 instructions / 140 functions across the seven globals, committed as research/data/selection-xrefs.tsv.
3. Q2: 111 constant occurrences with roles; only 43 are cap-relevant. research/data/selection-immediates.tsv.
4. Q3: answered per array. Four of five have ZERO slack, each with the neighbour identified by name.
5. Calibration: 3 of 5 functions compared in depth against GPTP ce321f0, agreements and disagreements both reported.

Two things I would want a reviewer to look at hardest:

- The sweep needed TWO passes. Ghidra records no reference at all for `MOV dword ptr [EDX*0x4 + 0x6284e8],ESI` at 0x004C26B6 — a scaled-index write into playersSelections. A single-pass table would have been silently incomplete in exactly the instruction class that matters most. The second pass also found ~20 sites of real code auto-analysis had left undefined (function count 5008 -> 5082).
- Four corrections to research/selection-cap.md, the biggest being that its §4.4 is wrong: `getActivePlayerNextSelection` (0x0049A850) is NOT bound-agnostic — `CMP BL,0xc` gates it. §4.4 calls that its "single most encouraging finding"; it does not hold. The function is called from 71 sites, so it is one fix, not 71.

Also resolved from the binary, which were open questions in §8: q5 (hotkey entries are StoredUnit u32s, GPTP right / teippi wrong) and q8 (the wire count is UNSIGNED — `JA`, not `JG` — so the protocol ceiling is 255, not 127; GPTP's s8 typing is behaviourally wrong).

Environment note, already sent separately and now acted on: Ghidra is installed at C:\re-tools\ghidra_12.1.2_PUBLIC and GHIDRA_INSTALL_DIR is set at user scope. My sweep.ps1 hardcodes no worktree path. I did NOT touch tools/ghidra/README.md — you said you would cut a follow-up for that.

Left open deliberately and listed in the doc: §8 q4's SC_memcpy_0 compaction (the HUD-copy half of q4 IS answered), q6, q7, q9, q10.
