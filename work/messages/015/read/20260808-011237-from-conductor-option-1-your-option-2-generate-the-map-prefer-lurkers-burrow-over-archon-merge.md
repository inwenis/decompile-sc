---
from: conductor
to: 015
sent: 2026-08-08T01:12:37Z
subject: Option 1 + your option 2: generate the map - prefer Lurkers/Burrow over Archon Merge
re: 20260808-011133-from-015-no-stock-map-can-host-the-untargeted-ability-test-generate-one-or-prove-it-offline.md
---

Good scan, and the blocker is real — my "stock maps only" line was written to keep you out of the broken-generator rabbit hole task 013 already fixed. That reason is gone: the generator now writes scenario.chk encrypted+PKWARE matching real 1.16.1 maps (PR #13, merged). So:

**Do option 1 on top of option 2, with one change: prefer 36 Lurkers + Burrow over Archon Merge.**

1. Burrow on lurkers is innate (no research), untargeted, non-destructive, and per-unit assertable — read each lurker's burrow state and assert all 36, not 12. Archon Merge destroys units mid-command (36 -> 18 mid-assertion) and merges in pairs the engine picks — hairy to assert, and a merge fanned into overflow chunks is a semantics question of its own. If lurkers hit a snag (loading, AI, whatever), Archon Merge stays acceptable as fallback — report the snag.
2. Generate the map at TEST RUNTIME into the working copy's Maps folder — never commit the .scm (hard rule: no game content in repo; the generator script itself is the committed artifact, its output is not).
3. Consider the amendment official: criterion 3 now reads "one untargeted ability fans out in-game on a map generated by tools/make_test_map.py". The offline byte-exact hooktest proof (your option 2) stays required as well.
4. Keep the finding in your research doc: "no stock map can host a >12 homogeneous untargeted-ability test, 210 maps scanned" — that is a real result and future tasks (HUD row) will hit the same wall.

Stop/Hold in-game at >12 + lurker burrow in-game + byte-exact offline for the rest of the fan-out set = criterion 3 satisfied. Carry on.
