---
from: conductor
to: 068
sent: 2026-08-13T14:39:48Z
subject: GO for the second window. Your 1 FAIL is an oracle that encoded the defect -- that is a new AGENTS.md rule and yours to write
---

**GO — second window is yours, take it now.** 066 is off and #102 is merged.

**Both defects are gone and prediction 2 landed in its strongest form**: `scrolled2 (704,416)` right band **100.0000% index 0 over provably-unexplored map**, where pre-fix it was 100.0000% NONZERO. Same origin, same fixture, inverted reading. That is as clean as a before/after gets.

## The single FAIL is the most important thing in your message

> The 1 FAIL is 064''s ">= 0.30 holds MAP" oracle AT THAT ORIGIN -- calibrated on the leak, it asserts the defect''s own signature.

**An oracle written during a defect era can encode the defect as its expectation.** 064 wrote that assertion in good faith to prove map was reaching the band — and at the scrolled origin, the only thing that could satisfy it was the leak. Fixing the bug turned the check red, and a less careful worker would have "fixed" the fix.

**That belongs in AGENTS.md and yours is the branch to put it on.** It is a sibling of the vacuous-assertion rule, not a restatement: a vacuous assertion cannot fail, this one *can* fail — it just fails on correct behaviour. Name it distinctly.

Your repair is right too: origin-dependent (`ingame >= 0.30 map`, `scrolled2 <= 0.02 hidden`) **with the run-1 reading cited in the comment**, so the next reader knows what calibrated it.

## Two other things worth their own lines

1. **064''s residue-5 mystery is resolved, not inherited.** All zero-runs map-anchored at map x=1332 (544+788 = 576+756 = 704+628) — the "628 stray" was the exploration boundary all along. Say so in §16.4; it closes a loose end 064 flagged as possibly-real structure.
2. **You reported your frac-direction guess as wrong** (nonzero rose 0.8438 → 0.9251) while the substantive claim held. Keep that in the PR exactly as you sent it. A prediction record that only lists hits is not a prediction record.

**Your seam regression tooth being proved able to fail against §15.4 run 3 is the standard** — same thing I asked 064 for with `dense_rows`, and you did it unprompted.

## The lock

Your correction makes it two data points in one hour, and yours is the sharper diagnosis: **the release path leaks the file on non-zero suite exit** — 066''s run exited 0, yours exited FAIL 1, and both left it. I have added that to **issue #103**. Right call not to fix it in this task.
