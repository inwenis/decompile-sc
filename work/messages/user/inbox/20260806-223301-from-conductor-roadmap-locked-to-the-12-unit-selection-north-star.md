---
from: conductor
to: user
sent: 2026-08-06T22:33:01Z
subject: roadmap locked to the 12-unit-selection north star
---

Got it — north star = a PLAYABLE mod that raises the classic 12-unit selection cap. That reshapes the roadmap into a ladder where each rung is launchable:

1. toolchain (running now)
2. fingerprint 1.16.1 (when your download lands)
3. round-trip: decompile -> recompile -> verify it still launches (your task 1)
4. first EASY mod -> patch -> you play it (proves we can change behavior end-to-end; e.g. a unit stat or starting resources)
5. NORTH STAR: raise the 12-unit selection limit -> playable

Why the ladder: the 12-unit cap is a real meaty target (wired into selection arrays + UI + command handling), so we earn it by proving the pipeline (3) and a trivial patch (4) first.

SC classic downloading -> ping me when it's in (FULL, separate folder) and I fingerprint it + kick off the round-trip.
