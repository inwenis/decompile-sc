---
from: conductor
to: user
sent: 2026-08-07T20:53:55Z
subject: STOP - do not run B. The map is broken, not the plugin. My error.
---

**Stop — do not run B. The fixture is broken, not the plugin.**

That map is ours, generated by task 009, and it is invalid. Nothing you did was wrong and the fan-out code is not implicated: the game rejected the file before any of our test could begin.

**This is my failure, specifically.** Task 009 validated the map *structurally* — parsed it back, confirmed 36 Marines, right owner, valid start location — and I accepted that as sufficient. It then asked whether it should load it in-game, and I told it that was optional and not worth reopening a session for. So the one check that mattered, "does StarCraft actually accept this file", was never performed by anyone. You performed it, mid-test, which is the worst possible place for it to surface.

Parsing a file you wrote with the same library that wrote it proves the library is self-consistent. It proves nothing about whether a 1998 game will load it. I should have caught that.

## What happens now

I am dispatching a fix with a different strategy: **find a stock Blizzard map that already has more than 12 units for the player.** The install ships ~200 maps including scenarios with large pre-placed armies. One of those is guaranteed loadable — no generator involved, no corruption risk — and we can scan them with the map library we already have without launching the game.

Fixing our own generator is worth doing too, but it is no longer on the critical path, and this time "the game loads it" will be an acceptance criterion rather than an optional extra.

## Meanwhile

Your two runs are still valid tests once there is a working map — hold them. Nothing is in a bad state: no game process is running, nothing was written into the game folder, and both your pristine install and the working copy's executable are unchanged.

If you are curious in the meantime, run A's log did capture the plugin attaching successfully before the map failed, so that half is confirmed working.
