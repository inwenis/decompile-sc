---
from: conductor
to: user
sent: 2026-08-12T21:02:48Z
subject: Primer running. And the keyboard idea failed -- measured, not assumed
---

Got it, thank you — 050 has the GO and is running the primer now.

Answering the thing I raised and should not leave hanging: **the keyboard idea did not work.** 050 probed it off-screen in about 40 seconds rather than theorising. Baseline `Custom Type` = Free For All; blind Down+Enter left it unchanged; Tab-then-Enter walked off the lobby screen entirely, so focus landed on something that was not the combo. Keyboard input does not reach that control on either path tried, and the foreground requirement stands.

I am recording it at that strength rather than as "the combo ignores the keyboard", because the probe never confirmed the combo can be focused at all — it only proved the two obvious routes fail. That is decisive enough to stop trying and not so strong that it stops someone with a better idea later.

The measured negative is going into the rulebook with the probe path, so nobody spends an afternoon rediscovering it. 050 has also rewritten the error message that used to say "re-run with `-Visible`" — it now names what `Custom Type` actually reads and the fact that one pick fixes every suite, which is the thing that was missing.

Next message from me will be whether the primer worked and whether all six suites then ran invisibly. After that 051 gets the machine — it needs Use Map Settings for its save/load fixture, which the primer hands it for free.
