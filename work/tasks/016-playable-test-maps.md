# Task 016 — Make generated maps playable; prove burrow fans out in-game

## Status

agent: 016
model: opus
pr: https://github.com/inwenis/decompile-sc/pull/16
merged: 2026-08-08

## Workspace

- repo: C:/git/decompile-sc (research + orchestration in one repo)
- worktree: C:/git/decompile-sc-task016 — conductor pre-created it; you were
  spawned inside it. ALL code/doc edits happen here, on branch task016-playable-test-maps.
- Task-file edits, reports, messages: directly in C:/git/decompile-sc (main
  checkout, no commits there — conductor commits; you are the only writer).
- NEVER edit tracked content in the main checkout.
- Destructive/irreversible actions beyond your own worktree (deleting user
  files, force-push, merging PRs, killing processes) → stop, ask in your tab
  or message the conductor first.
- NEVER delete/overwrite anything under C:/git/decompile-sc/work/messages/
  (2026-07-17 data-loss incident class — real user messages live there).
- C:/git/conductor and C:/git/conductor-task* are ANOTHER LIVE SYSTEM (a
  separate orchestrator with in-flight agents and real user messages).
  NEVER read, modify, cd into, or run git/gh against them — off-limits
  absolutely (guard hooks also enforce this). Your world is
  C:/git/decompile-sc and your own worktree only.

## Game-file rules (project hard rules)

- NEVER commit game binaries, MPQ archives, extracted assets, or anything
  derived from them that reproduces game content. The repo tracks findings
  and tooling only.
- The local game copy lives at an ignored path (game/) and is READ-ONLY to
  workers.
- Never point a modified binary at Battle.net or any online service. Offline
  and single-player only.
- Every claimed address/offset/struct must carry evidence: how it was found
  and how it was verified. No guessed offsets in research/.

## Messaging

- Your inbox: `C:/git/decompile-sc/work/messages/016/inbox/` (`read/` next to it).
- Run every `.ps1` (send-message, etc.) via the PowerShell tool, NEVER the
  Bash tool — Bash invokes Windows PowerShell 5.1, `#Requires -Version 7`
  fails, NOTHING is written, and the call LOOKS sent (message-loss class,
  2026-07-18: tasks 041/054 "lost" pictures were never-written sends).
  Always check the script printed the written file path.
- FIRST duty, before any other work: arm a monitor on that inbox (harness
  `Monitor` tool + poll loop — see AGENTS.md § Messaging), then tell the
  conductor you are listening:
  `./scripts/send-message.ps1 -To conductor -From 016 -Subject READY -Body '<one line>'`
  (run from C:/git/decompile-sc)
- A from-CONDUCTOR message arrives → apply it, move it to `read/`, re-arm,
  carry on. A from-USER file is INFORMATIONAL: leave it, do not act — the
  conductor reviews every user message and relays instructions
  (AGENTS.md § Messaging).
- Questions or blockers → message `conductor`; never stall silently.

## Goal

**A map generated by `tools/make_test_map.py` plays as a real Use Map Settings game — placed
units spawn, the mission does not auto-end — and on such a map the in-game burrow proof runs:
box >12 lurkers, one keypress, every lurker burrows.** This closes the half of task 015's
criterion 3 the conductor waived (waiver recorded in `research/command-opcodes.md` §8): the
fan-out of an untargeted ability proven in the live engine, not only byte-exact offline.

## Context

- The two known failures, written up by task 015 — start there, do not rediscover:
  - `research/command-opcodes.md` §8/§8.1 — melee-template maps play as MELEE even with Game
    Type = Use Map Settings (placed units never created; proven from in-process unit reads:
    four Drones, three Larva, one Overlord, a Hatchery where the file holds 36 lurkers).
    Campaign-template maps load, brief, enter the mission, then END within ~7 s — two different
    campaign templates identical, suspicion: CHK round-trip disturbs TRIGGER data.
  - `tools/README-test-map.md` ("THE BIG ONE") — same story from the generator side.
- Already fixed by task 015, do not redo: `OWNR` `0x06` (was the "no slot for a human
  participant" refusal), unit block centred on the start location.
- Task 013 fixed scenario.chk writing (encrypted+PKWARE, matching real 1.16.1 maps) — the
  CONTAINER is right; the suspect is now the CHK section CONTENT (TRIG/MBRF/FORC or a
  section-ordering/duplication subtlety). `richchk` reads maps fine (015 scanned all 210 stock
  maps with it) — diff a generated map's sections against a working stock UMS campaign map
  field by field; the delta that matters should be visible.
- The burrow assertion is already plumbed: the plugin logs `burrowed=N/M`
  (`SC_UNIT_FLAG_BURROWED`, `sc_addresses.h`), `test-fanout-orders.ps1` parses it into a
  `Burrowed` property — nothing asserts on it yet. Add the in-game step: spawn map with 36
  Lurkers (burrow is innate, no research), box them, press burrow, assert burrowed goes
  0/36 → 36/36. Follow the test discipline task 015's fix round established: pinned order ids,
  single-bucket/positive asserts, no arrival-ambiguity windows, launch inside try, close by
  -ProcessId, SHA-256 before/after.
- Generated maps are TEST FIXTURES: generate at test runtime into the working copy's Maps
  folder, delete after, never commit a `.scm`/`.scx`. The generator script is the artifact.
- Fan-out set, opcode table, replay mechanics: `research/command-opcodes.md`,
  `tools/plugin/src/sc_fanout.cpp` — all merged; base your worktree work on them as-is. Burrow
  (`0x2C`? — read the id from `research/data/command-opcodes.tsv`, do not trust this line) is
  already in the 19-id fan-out set; this task proves it in-engine, it does not change the set.
- Driving the game: `tools/plugin/drive-game.ps1` (posted messages only; `SendInput`/`SendKeys`
  banned). Menu path for custom maps and the map-browse recipe are in
  `test-selection-circles.ps1` / `test-fanout-orders.ps1`.
- Verified constants: module base `0x00400000`, delta zero, `CUnit` 336 bytes, pristine hash
  `AD6B58B2…C6A46`.

## Hard rules

1. Never modify, write to, or launch `C:\sc-install\Starcraft`. Working copy only.
2. Offline, single-player only. Never Battle.net.
3. `StarCraft.exe` on disk stays byte-identical; tests assert it.
4. Passive mode stays stock.
5. Commit no game content, no binaries, no logs, no screenshots, no generated maps.
6. Do not merge your own PR.

## Acceptance criteria

1. Root cause of BOTH template failures identified with evidence (field-level CHK diff or
   in-engine observation): why melee-template output plays as melee, and what ends
   campaign-template missions at ~7 s. "Fixed but do not know why" does not close this task.
2. `make_test_map.py` produces a map that: loads via Play Custom as UMS, spawns exactly the
   placed units (verified in-process), and idles indefinitely (no auto-end within ≥120 s).
3. In-game burrow proof on that map: >12 lurkers, one keypress, `burrowed` asserted 0/N → N/N
   with N > 12, unattended, using the 015-era test discipline.
4. Regressions green: `build.ps1 -Test`, `test-fanout-orders.ps1`, `test-selection-circles.ps1`;
   exe byte-identical before/after; no stranded processes; CI green.
5. `research/command-opcodes.md` §8 waiver block updated: criterion closed by this task, with
   the run evidence. `tools/README-test-map.md` "THE BIG ONE" resolved with the root cause.
6. PR opened, link in Status.pr.

## Reporting

Status is DERIVED, never reported. The console works out what this task is
doing from things that already exist — the agent registry, the PR, the
`merged:` stamp, your open questions — so there is no `state:` line to keep
current and no status to update. You have exactly three duties:

- Open a PR when you have something to review, and put its link in Status.pr
  (an open PR is what makes this task read as "review").
- <either> that PR <or> a report at work/reports/016-playable-test-maps.md — UNLESS the
  Goal/Context above says otherwise, in which case that wins. This section is
  generic scaffolding; a task-specific instruction (or a standing user rule
  quoted in it) always overrides it. Do not "satisfy both" by writing a file
  the task told you not to write.
- Blocked? Ask — `send-message.ps1 -Type question` (§ Messaging). An unanswered
  question of yours IS the blocked signal; answering it clears it. Never write
  a status instead of asking.
- UI-visible changes → screenshot → `pr-image <file>` → embed markdown in PR.
